# Baseline Exclusions

**Document:** Q-Sight Command Center — Pre-Remediation Baseline Exclusions  
**Date:** 2026-07-13  
**Author:** Q-Sight Governance Bot (Agent A1)  

---

## Summary

The following files and directories were **intentionally excluded** from the baseline commit and SHA256 manifest. This document records the rationale for each exclusion category.

---

## Exclusion Categories

### 1. `.env` — Credential File — NEVER COMMIT

| Field | Value |
|-------|-------|
| File | `D:\Q-Sight Command Center\.env` |
| Size | 2,790 bytes |
| Reason | Contains live credentials: database passwords, API keys, JWT secrets |
| Protection | Listed on line 22 of `.gitignore` — `git add .` will NOT stage this file |
| Verification | SHA256 manifest does NOT contain `.env` — confirmed |
| Action | File exists locally for runtime use. NEVER push to any remote. |

**Credential categories present (values NOT printed):**
- `DATABASE_URL` — PostgreSQL connection string with password
- `JWT_SECRET` — Authentication signing key
- Third-party API keys (OpenSky, etc.)

---

### 2. `node_modules/` — Dependency Directory

| Field | Value |
|-------|-------|
| Pattern | `node_modules/` (all nested occurrences) |
| Reason | ~300–1000MB of third-party packages; reproducible via `npm ci` |
| Protection | Listed in `.gitignore` as `node_modules/` |
| Standard | Industry standard — never commit node_modules |

---

### 3. `dist/` — Compiled Output

| Field | Value |
|-------|-------|
| Pattern | `dist/` directories under `apps/api/`, `apps/web/`, `workers/*/` |
| Reason | Compiled TypeScript output; fully reproducible via `npm run build` |
| Protection | Listed in `.gitignore` as `dist/` |
| Note | Source files are committed; dist is a build artifact |

---

### 4. `build/` — Build Artifacts

| Field | Value |
|-------|-------|
| Pattern | `build/` directories |
| Reason | Build output directories |
| Protection | Listed in `.gitignore` as `build/` |

---

### 5. `.next/` — Next.js Build Cache

| Field | Value |
|-------|-------|
| Pattern | `.next/` |
| Reason | Framework build cache; excluded as precaution |
| Protection | Listed in `.gitignore` as `.next/` |

---

### 6. `*.tsbuildinfo` — TypeScript Build Incremental Cache

| Field | Value |
|-------|-------|
| Pattern | `*.tsbuildinfo` files |
| Reason | These ARE included in the baseline commit (not gitignored) — they record TypeScript build state at baseline time |
| Note | They appear in BASELINE_FILE_MANIFEST.sha256. Deliberate decision to capture full build state. |

---

### 7. `.env.*.local` — Local Environment Overrides

| Field | Value |
|-------|-------|
| Pattern | `.env.local`, `.env.development.local`, `.env.test.local`, `.env.production.local`, `.env.*.local` |
| Reason | Local overrides may contain developer-specific secrets |
| Protection | All patterns listed in `.gitignore` lines 23–27 |
| Note | `.env.demo.example`, `.env.example`, `.env.live.example`, `.env.staging.example` — COMMITTED as they are templates without secrets |

---

### 8. IDE / Editor Files

| Field | Value |
|-------|-------|
| Pattern | `.idea/`, `.vscode/`, `*.suo`, `*.sln`, etc. |
| Reason | Developer-local configuration; not part of the project |
| Protection | Listed in `.gitignore` lines 29–36 |

---

### 9. OS Metadata

| Field | Value |
|-------|-------|
| Pattern | `.DS_Store`, `Thumbs.db` |
| Reason | Operating system metadata artifacts |
| Protection | Listed in `.gitignore` lines 38–40 |

---

## Verification

```
.env present in .gitignore:     YES (line 22)
.env present in manifest:       NO  (confirmed absent)
.env staged in git:             NOT STAGED (protected by .gitignore)
node_modules in manifest:       NO  (excluded from scan)
dist/ in manifest:              NO  (excluded from scan)
```

---

## Files Committed Despite Being Build Artifacts

The following files ARE committed intentionally (they are NOT gitignored):

- `*.tsbuildinfo` files — capture compiler state at prototype baseline
- `package-lock.json` — locked dependency tree (npm best practice)
- `docs/audit/audit_*.json` — audit records
- `docs/audit/test_results_*.txt` — test evidence

---

*Generated by Agent A1 — Q-Sight P0 Hardening Program — 2026-07-13*
